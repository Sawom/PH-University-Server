import { z } from "zod";
// with zod
// model er field gula ekhaneo use hobe
const createAcademicSemesterValidationSchema = z.object({
  body: 
});

export const AcademicSemesterValidations = {
  createAcademicSemesterValidationSchema,
};
